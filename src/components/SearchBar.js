import React from "react";

const SearchBar = () => {
  return (
    <section>
      <form>
        <input value={"Search Github"}></input>
      </form>
    </section>
  );
};

export default SearchBar;
